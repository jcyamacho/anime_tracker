import 'package:flutter/material.dart';

class ErrorDetail extends StatelessWidget {
  const ErrorDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error!'),
    );
  }
}
